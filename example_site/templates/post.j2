{% extends "base.j2" %}

{% block title %}
{{ post.frontmatter.title }} · {{ site.title }}
{% endblock %}

{% block content %}
<article>
    <h2>{{ post.frontmatter.title }}</h2>
    <p class="date">Published on {{ post.frontmatter.date }}</p>

    {{ post.content | safe }}
</article>
{% endblock %}
